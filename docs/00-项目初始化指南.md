# 项目初始化指南 - 完全初学者版

## 📚 什么是这个项目？

这是一个**网页版2D平台跳跃游戏**，特别之处在于：
- 游戏能"学习"物理规律
- 能**预测**你按下按键后会发生什么
- 在屏幕上实时显示预测结果

---

## 🛠️ 需要安装的工具

### 1. Node.js（JavaScript运行环境）
**是什么？** 让你的电脑能运行JavaScript代码的工具

**下载安装：**
```bash
# 访问 https://nodejs.org/
# 下载 LTS 版本（推荐版本）
# 安装后验证：
node --version   # 应该显示 v18.x.x 或更高
npm --version    # 应该显示 9.x.x 或更高
```

### 2. Python（用于训练模型）
**是什么？** 机器学习最常用的编程语言

**下载安装：**
```bash
# 访问 https://www.python.org/
# 下载 Python 3.10 或 3.11
# 安装后验证：
python --version  # 应该显示 Python 3.10.x 或 3.11.x
pip --version     # 应该显示 pip 22.x 或更高
```

### 3. 代码编辑器（VSCode 推荐）
**是什么？** 专业的代码编辑软件

**下载安装：**
```bash
# 访问 https://code.visualstudio.com/
# 下载并安装

# 推荐安装的插件：
- ESLint（代码检查）
- Prettier（代码格式化）
- Python（Python支持）
```

---

## 📦 项目依赖包说明

### package.json（Node.js项目配置文件）

```json
{
  "name": "mirror-chunyun",
  "version": "1.0.0",
  "description": "2D平台游戏 + 世界模型Demo",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "collect-data": "vite --mode collector"
  },
  "dependencies": {
    "phaser": "^3.80.1",
    "@tensorflow/tfjs": "^4.17.0"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "vite": "^5.0.11",
    "@types/node": "^20.11.5"
  }
}
```

**依赖包解释：**

| 包名 | 用途 | 通俗解释 |
|------|------|----------|
| **phaser** | 游戏引擎 | 帮你处理游戏画面、物理碰撞、键盘输入的工具 |
| **@tensorflow/tfjs** | 机器学习库 | 在浏览器里运行AI模型的工具 |
| **typescript** | 编程语言 | 比JavaScript更严格的语言，能提前发现错误 |
| **vite** | 构建工具 | 快速启动开发服务器、打包代码的工具 |

---

## 🚀 初始化步骤

### Step 1: 创建项目文件夹
```bash
cd /home/user/mirror_chunyun
```

### Step 2: 初始化Node.js项目
```bash
npm install
```

**会发生什么？**
- 读取 `package.json` 文件
- 下载所有依赖包到 `node_modules/` 文件夹
- 生成 `package-lock.json`（锁定依赖版本）

### Step 3: 创建 TypeScript 配置
**文件：** `tsconfig.json`

```json
{
  "compilerOptions": {
    "target": "ES2020",
    "module": "ESNext",
    "lib": ["ES2020", "DOM"],
    "moduleResolution": "bundler",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "outDir": "./dist"
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules"]
}
```

**配置解释：**
- `target`: 编译成什么版本的JavaScript
- `strict`: 开启严格模式（能发现更多错误）
- `include`: 哪些文件需要编译

### Step 4: 创建 Vite 配置
**文件：** `vite.config.ts`

```typescript
import { defineConfig } from 'vite';
import path from 'path';

export default defineConfig({
  resolve: {
    alias: {
      '@': path.resolve(__dirname, './src'),
    },
  },
  server: {
    port: 3000,
    open: true, // 自动打开浏览器
  },
  build: {
    outDir: 'dist',
    sourcemap: true, // 生成源码映射（方便调试）
  },
});
```

**配置解释：**
- `alias`: 用 `@` 代表 `src` 目录（简化导入路径）
- `port`: 开发服务器端口号
- `sourcemap`: 方便调试的工具

### Step 5: 创建 HTML 入口
**文件：** `index.html`

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>2D平台游戏 - 世界模型Demo</title>
  <style>
    body {
      margin: 0;
      padding: 0;
      background: #1a1a1a;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      font-family: Arial, sans-serif;
    }
    #game-container {
      box-shadow: 0 0 20px rgba(0, 0, 0, 0.5);
    }
  </style>
</head>
<body>
  <div id="game-container"></div>
  <script type="module" src="/src/main.ts"></script>
</body>
</html>
```

### Step 6: 创建 Python 环境（训练用）
```bash
cd train
python -m venv venv  # 创建虚拟环境

# 激活虚拟环境
# Windows:
venv\Scripts\activate
# Mac/Linux:
source venv/bin/activate

# 安装依赖
pip install tensorflow numpy
```

---

## 📂 项目目录结构

```
mirror_chunyun/
├─ src/                     # 源代码目录
│  ├─ main.ts              # 入口文件
│  ├─ game/                # 游戏逻辑
│  ├─ model/               # AI模型
│  ├─ tools/               # 工具脚本
│  └─ ui/                  # 用户界面
├─ train/                   # 训练脚本
│  ├─ venv/                # Python虚拟环境
│  ├─ train.py             # 训练主脚本
│  └─ requirements.txt     # Python依赖
├─ public/                  # 静态资源
│  ├─ models/              # 训练好的模型
│  └─ assets/              # 图片、音频
├─ data/                    # 训练数据
├─ docs/                    # 设计文档
├─ node_modules/            # Node.js依赖（自动生成）
├─ dist/                    # 构建输出（自动生成）
├─ index.html              # HTML入口
├─ package.json            # 项目配置
├─ tsconfig.json           # TypeScript配置
└─ vite.config.ts          # Vite配置
```

---

## ✅ 验证安装

运行以下命令测试环境：

```bash
# 1. 启动开发服务器
npm run dev

# 应该看到：
# VITE v5.x.x ready in xxx ms
# ➜ Local:   http://localhost:3000/
# 浏览器自动打开
```

如果看到空白页面但没有报错，说明环境配置成功！

---

## 🎯 下一步

环境配置完成后，继续阅读：
- `01-Module1-基础游戏引擎.md`
